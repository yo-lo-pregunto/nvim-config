-- One day it will work but not for now (required nvim 0.10.0-dev) which I not
-- going to installed.
return {
    -- 'Bekaboo/dropbar.nvim',
    -- -- optional, but required for fuzzy finder support
    -- dependencies = {
    --     'nvim-telescope/telescope-fzf-native.nvim'
    -- }
}
